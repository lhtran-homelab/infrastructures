apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: zerossl-lhtran-com
  annotations:
    argocd.argoproj.io/sync-wave: "1"  
spec:
  acme:
    email: longhtran91@gmail.com
    server: https://acme.zerossl.com/v2/DV90
    externalAccountBinding:
      keyID: WeWdjQ3xGyOzWN8iAcpUyg
      keySecretRef:
        name: zerossl-eabsecret
        key: secret
    privateKeySecretRef:
      name: zerossl-account-key
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-api-token-secret
              key: api-token